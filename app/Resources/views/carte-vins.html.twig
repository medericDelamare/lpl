{% extends 'base.html.twig' %}

{% block body %}
    <div class="parallax-menu"></div>
    <section class="container title-container">
        <div class="title-page">
            <h2> Carte des vins </h2>
        </div>
    </section>

    <div id="carte">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    {% for key,typeVin in typesVins %}
                        {% if(key%2 == 0) %}
                            <div class="m-categorie">
                                <div class="m-title-categorie">
                                    <h4 class="text-center couleur-principale">{{ typeVin.nom }}</h4>
                                </div>
                                {% for vin in typeVin.vins %}
                                    <div class="m-plat-info">
                                        <div class="m-titre-plat">
                                            <span class="m-nom-plat m-photo-active">{{ vin.nom }}</span>
                                        </div>
                                        <span class="m-separateur"></span>
                                        <div class="m-prix">
                                            <div class="m-quantite">
                                                {% if typeVin.code != 'pichet' %}
                                                    <span>75cl</span>
                                                {% endif %}
                                                <span>{{ vin.prix1 }}€</span>
                                            </div>
                                            {% if typeVin.code != 'pichet' %}
                                                <div class="m-quantite">
                                                    {% if typeVin.code != 'pichet' %}
                                                        <span>37,5cl</span>
                                                    {% endif %}
                                                    {% if vin.prix2 %}<span>{{ vin.prix2 }}€</span>{% else %}<span>-</span>{% endif %}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="col-md-6">
                    {% for key,typeVin in typesVins %}
                        {% if(key%2 != 0) %}
                            <div class="m-categorie">
                                <div class="m-title-categorie">
                                    <h4 class="text-center couleur-principale">{{ typeVin.nom }}</h4>
                                </div>
                                {% for vin in typeVin.vins %}
                                    <div class="m-plat-info">
                                        <div class="m-titre-plat">
                                            <span class="m-nom-plat m-photo-active">{{ vin.nom }}</span>
                                        </div>
                                        <span class="m-separateur"></span>
                                        <div class="m-prix">
                                            <div class="m-quantite">
                                                {% if typeVin.code != 'pichet' %}
                                                    <span>75cl</span>
                                                {% endif %}
                                                <span>{{ vin.prix1 }}€</span>
                                            </div>
                                            {% if typeVin.code != 'pichet' %}
                                                <div class="m-quantite">
                                                    {% if typeVin.code != 'pichet' %}
                                                        <span>37,5cl</span>
                                                    {% endif %}
                                                    {% if vin.prix2 %}<span>{{ vin.prix2 }}€</span>{% else %}<span>-</span>{% endif %}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}